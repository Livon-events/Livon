# FR Docs

Drop your feature-requirement docs here, one file per feature — same
naming/structure you've already used (auth, connections, event-creation,
event-details, feed-ranking, going-rsvp-privacy, home-feed-eventcard,
invite-links, location-toggle, peek, user-profile, event-lifecycle,
category-filtering, search).

Also put `architecture.md` directly in this `docs/` folder (or here in
`docs/fr/` if you'd rather keep it alongside the feature docs — your call,
just be consistent) so it's readable from the project root by Claude Code.
